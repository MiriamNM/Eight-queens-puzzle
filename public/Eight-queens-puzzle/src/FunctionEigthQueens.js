// import react from 'react';

const FunctionEigthQueens = (inputValue) => {
    /*Restricciones
    Una reina por cuadricula
    Una reina por fila
    Una reina por diagonal
    #fila + #columna=== c/reina && queens(j) + j == en 2 indices diferentes de j.
    #fila - #columna=== c/reina && queens(j) - j == en 2 indices diferentes de j.
    Las reinas se encuentran en la misma diagonal ascendente de izquierta a derecha.
    Las reinas se encuentran en la misma diagonal descendiente.
    Valores de queens(j) + j son todos diferentes para j
    Valores de queens(j) - j son todos diferentes para j

    */
}
FunctionEigthQueens(8)